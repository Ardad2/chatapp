import {initializeApp} from 'firebase';
import { getDatabase, ref, set, onValue } from 'firebase/database';

function App() {
  return (
    <div className="App">
      <h1>Chat App</h1>
    </div>
  );
}

export default App;
